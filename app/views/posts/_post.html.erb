
<li>
<%= avatar_for(@post.user,50,50) %>
<span class="user">
 <%= link_to(@post.user.name, @post.user) %>
</span>

  <span class="content"><%= post.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(post.created_at) %> ago.
  </span>

<% if signed_in? %>
  <% if current_user?(post.user) %>
    <%= link_to "Eliminar", post, method: :delete, "data-ajax"=>"false",
                                     data: { confirm: "¿Seguro?" },
                                     title: post.content %>
  <% end %>
<% end %>
</li>