<%= form_for @post do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Comenta lo que sucede en el lugar..." %>
  </div>
  <%= f.submit "Enviar", class: "btn btn-large btn-primary" %>
<% end %>